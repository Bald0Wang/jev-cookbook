# 验证记录

## 0.1.2 真实联调（2026-09-24）

真实 Jev 原语与三条固定分流案例通过；DeepSeek 主模型经 DSH 核心运行时完成实际修复和澄清后继续。补强了首步分流及澄清等待规则，26 项离线测试通过。初次澄清跳过、headless 未挂载模式和网络失败记录均保留。

证据与复现步骤见[真实联调记录](LIVE-VALIDATION-20260924.md)。网页人工交互、跨机器复现及网络稳定性尚未覆盖。下文 0.1.1 的“未完成”为历史状态。

## 0.1.1 补齐记录（2026-09-23）

范围仍为原任务“1 人 1 天的 DSH 决策插件 / 新增模式”。本版增加真实分流检查与报告、模式后续行为提示、独立聊天练习和最小验收指南，没有扩展为业务评测平台。

- Node.js `24.19.0` 下 `npm test`：26 项通过，0 项失败或跳过。
- 三条离线演示通过 DSH ToolRuntime 执行；人工夹具标识保持不变。
- 新增联调测试覆盖：缺少凭据不联网、预期标签不发送给 API、返回分支不同仍保留原始结果、限流停止、报告拒绝覆盖。网络响应在测试中被替换，不代表真实模型通过验收。
- 实际运行缺少凭据的 `check-live.mjs --routes --output ...`：退出码 1，报告为 `missing_api_key`，`request_attempts=0`，跳过剩余两个案例。该检查仅证明缺失配置路径。
- 聊天练习起点的现有 ISO 日期测试通过。空字符串缺陷有意保留给 DSH 演示任务处理，尚未由真实聊天主模型修复。
- 文档本地链接、代码围栏、版本与锁文件一致性检查通过。
- 新版官方安装验收 `npm run test:install` 通过：临时 npm 包经 DSH CLI 安装，合成配置正确，模式可发现并完整挂载，已安装工具可执行。首次受限环境中的 pnpm 依赖安装失败，允许依赖下载后完整重跑通过；未调用付费模型。

**尚未完成：真实 TypeSafe 推理和真实 DeepSeek 主模型完整聊天。** 本轮开始时进程未配置两套 Key，项目中也没有 `.env`；无凭据的验证没有产生模型请求。后续按 [ACCEPTANCE.md](ACCEPTANCE.md) 配置并保存实测证据。

## 0.1.0 原验收记录

验收日期：2026-09-23。以下记录对应 `dsh-jev-decision@0.1.0`。

## 验收环境

- macOS，Node.js `24.19.0`，pnpm `11.19.0`。
- DSH 及相关工具包 `0.1.5-rc.2`，Cordis `4.0.2`。
- 使用独立的项目/临时 DSH home；未修改用户默认 `~/.dsh`。
- 安装依赖时关闭生命周期脚本；未调用付费模型接口。

## 已通过

| 检查 | 结果与范围 |
| --- | --- |
| TypeScript 编译 | `npm run build` 通过，严格类型检查 |
| 自动化测试 | `npm test`：22 项通过，0 项失败或跳过 |
| 三条离线演示 | `npm run demo`：通过真实 DSH ToolRuntime 执行 `proceed`、`clarify`、`review` 人工夹具 |
| 安装包内容 | 自动检查编译入口、bundle patch、模式文件、安装器与示例 JSON |
| 官方安装命令 | 在临时 home 创建 web profile，运行 `dsh plugin ... add <tgz>` 成功 |
| 配置合成 | profile 的 bundle 列表与合成配置均包含本插件 |
| 安装器 | 从已安装 npm 包运行模式复制脚本，成功写入正确目录 |
| Web 入口 | DSH Web profile 的 `--help` 成功，完成首次启动所需的宿主依赖链接 |
| 完整模式挂载 | 通过 DSH 的 `discoverPresets` 与 `mountPreset`，在完整 `dsh-base` core 下加载全部模式行和提示词 |
| 安装后执行 | 通过 Loader 加载已安装插件，并在真实 ToolRuntime 中执行三种原语的人工响应 |

自动化测试包含：结构化输入与快照、空值/循环/体积/题数限制、概率和与 Score 期望校验、缺失密钥、HTTP 400/401/403/429/503、网络错误、不回显服务端错误正文、无重试、超时、取消、响应大小限制、工具注册/渲染/卸载、作用域覆盖、宿主策略拒绝、失败不生成路由建议、阈值边界、安装器拒绝覆盖及符号链接入口。

安装验收命令为 `npm run test:install`。它会生成临时 npm 包，通过官方 CLI 安装，再用实际 DSH core 验证模式挂载和工具执行，最后清理自己创建的临时目录。该步骤需要 pnpm 与下载依赖的网络，但不需要模型 Key。

## 尚未验证

- 真实 TypeSafe 账户下的 Jev 推理、账单与生产延迟。
- 真实 DeepSeek 主模型在 DSH 聊天界面中自主选择 Jev 工具的完整对话。
- 真实业务任务集上的分类质量、澄清率与置信阈值校准。
- Windows / Linux 与其他 DSH 版本。

这些限制不影响离线代码路径、打包安装和模式加载验收。配置自己的密钥后，按 README 的 `npm run check:live` 与 DSH 聊天步骤完成真实联调；不要把模拟数据当成实际推理结果。

## 验收中修复的问题

完整挂载检查发现两个 DSH 预设行缺少必填配置，已补齐。压缩包安装检查还发现 macOS `/var` 与 `/private/var` 路径别名使 CLI 主入口判断失效，现改为比较真实路径，并加入回归测试。预设发现检查安排在 DSH 首次启动建立依赖链接之后，与实际加载顺序一致。
