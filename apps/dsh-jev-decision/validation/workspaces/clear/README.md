# DSH × Jev 聊天演示材料

这是隔离的教学目录。演示前复制整个目录到新的工作目录，并让 DSH 使用那个副本。

当前 `parseDate` 对正常 ISO 日期返回 Date，但对空字符串返回 Invalid Date。演示需求是让空字符串返回 `null`，同时保持正常日期行为，并补回归测试。缺陷是特意保留的练习起点。

运行现有测试：`node --test date.test.mjs`。演示后应新增空字符串用例并通过。

`description.md` 需要同步说明空字符串处理规则。修改范围仅为本目录的源码、测试和说明。
